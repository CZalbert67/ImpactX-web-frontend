import { ImpactXLogo } from "@/components/branding/ImpactXLogo";

/** Logo para pantallas de marca (login/registro): versión ampliada. */
export function AppLogo() {
  return <ImpactXLogo size={38} />;
}